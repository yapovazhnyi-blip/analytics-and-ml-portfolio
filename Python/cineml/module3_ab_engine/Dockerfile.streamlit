FROM python:3.11-slim
WORKDIR /app
RUN apt-get update && apt-get install -y --no-install-recommends gcc g++ && rm -rf /var/lib/apt/lists/*
RUN pip install --no-cache-dir streamlit plotly pandas pyarrow numpy scipy statsmodels scikit-learn
COPY . ./module3_ab_engine/
ENV PYTHONPATH=/app
EXPOSE 8503
